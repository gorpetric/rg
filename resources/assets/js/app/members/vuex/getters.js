export const loading = (state) => state.loading

export const members = (state) => state.members

export const membership_monthly = (state) => state.membership_monthly
export const membership_daily = (state) => state.membership_daily

export const currencies = (state) => state.currencies
